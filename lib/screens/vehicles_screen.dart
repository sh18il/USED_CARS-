import 'package:flutter/material.dart';

class Vehicles_screen extends StatelessWidget {
  const Vehicles_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          Text('vehcles'),
        ],
      ),
    );
  }
}
